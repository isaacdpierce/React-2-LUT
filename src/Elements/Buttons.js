import styled from 'styled-components';
import { purp } from 'Utilities';

export const StyledButton = styled.button`
border: none;
  width: 300px;
  margin-bottom: 15px;
  height: 30px
  background-color: ${purp};

`;
